# 🚀 Руководство по запуску Subscription App

## Предварительные требования

- **Flutter SDK**: версия 3.10.4 или выше
- **Dart SDK**: входит в состав Flutter
- **IDE**: Android Studio, VS Code или Xcode
- **Платформа**: iOS или Android

### Проверка установки Flutter

```bash
flutter --version
dart --version
```

## 📋 Пошаговая инструкция по запуску

### 1. Клонирование/открытие проекта

```bash
cd mst_test_app
```

### 2. Получение зависимостей

```bash
flutter pub get
```

Эта команда загрузит все необходимые пакеты, включая:
- `shared_preferences` - для сохранения данных на устройстве

### 3. Запуск на эмуляторе или устройстве

#### Для Android:

```bash
# Убедитесь, что эмулятор Android запущен или устройство подключено
adb devices

# Запустить приложение
flutter run
```

#### Для iOS:

```bash
# Запустить на симуляторе iOS
open -a Simulator

# Или запустить приложение
flutter run -d iPhone
```

### 4. Запуск в режиме разработки

```bash
flutter run -v
```

Флаг `-v` показывает подробные логи, что помогает при отладке.

### 5. Hot Reload (горячая перезагрузка)

После запуска приложения:
- Нажмите `r` в консоли для hot reload (быстрая перезагрузка)
- Нажмите `R` для hot restart (полный перезапуск)
- Нажмите `q` для выхода

## 🏗️ Сборка приложения

### Для Android

```bash
# Debug APK
flutter build apk --debug

# Release APK
flutter build apk --release

# App Bundle
flutter build appbundle --release
```

### Для iOS

```bash
# Debug IPA
flutter build ios --debug

# Release IPA
flutter build ios --release
```

## 🧪 Тестирование приложения

### Структура папок

```
mst_test_app/
├── lib/
│   ├── main.dart                          # Точка входа
│   ├── constants/
│   │   └── colors.dart                    # Цвета
│   ├── models/
│   │   └── subscription.dart              # Модель подписки
│   ├── screens/
│   │   ├── onboarding_screen.dart         # Онбординг
│   │   ├── paywall_screen.dart            # Выбор подписки
│   │   └── home_screen.dart               # Главный экран
│   ├── services/
│   │   └── subscription_service.dart      # Сервис хранилища
│   └── widgets/
│       └── subscription_card.dart         # Карточка подписки
├── pubspec.yaml                           # Зависимости
└── README.md                              # Описание проекта
```

### Сценарии тестирования

#### 1. Первый запуск
- [ ] Приложение показывает экран онбординга
- [ ] Видны 2 экрана с иконками и описанием
- [ ] Точки навигации работают
- [ ] Кнопка "Продолжить" работает

#### 2. Экран Paywall
- [ ] Показываются две карточки подписок
- [ ] Месячная: 299 ₽
- [ ] Годовая: 2 490 ₽ со скидкой
- [ ] Выбор карточки подсвечивает её
- [ ] Кнопка "Продолжить" включается после выбора
- [ ] При нажатии показывается загрузчик

#### 3. Главный экран
- [ ] Отображается информация о подписке
- [ ] Видна дата активации
- [ ] Видна дата следующего платежа
- [ ] Показывается периодичность платежей
- [ ] Рассчитывается количество дней до платежа
- [ ] Отображаются преимущества подписки

#### 4. Сохранение данных
- [ ] Закройте приложение (swipe up на iOS, back button на Android)
- [ ] Заново откройте приложение
- [ ] Должна загрузиться подписка без онбординга

#### 5. Выход из аккаунта
- [ ] Нажмите кнопку logout (иконка выхода в правом углу)
- [ ] Подтвердите выход
- [ ] Должны вернуться на экран онбординга
- [ ] SharedPreferences должны очиститься

## 🐛 Решение проблем

### Проблема: "Doctor found issues"

```bash
flutter doctor
```

Решение проблем, указанных в выводе команды.

### Проблема: Зависимости не загружаются

```bash
flutter pub get
# или
flutter pub upgrade
```

### Проблема: Приложение не запускается на эмуляторе

```bash
# Перезагрузить эмулятор
flutter clean
flutter pub get
flutter run
```

### Проблема: Ошибки при сборке iOS

```bash
cd ios
pod deintegrate
pod install
cd ..
flutter clean
flutter run
```

### Проблема: SharedPreferences не работает на эмуляторе

Убедитесь, что:
1. Приложение имеет права на запись (в Android Manifest)
2. Эмулятор имеет достаточно места на диске
3. Очистите кэш: `flutter clean`

## 📱 Тестирование на реальном устройстве

### Android

```bash
# Проверить подключение
adb devices

# Установить APK
adb install build/app/outputs/apk/debug/app-debug.apk

# Запустить приложение через Flutter
flutter run
```

### iOS

```bash
# Подключить устройство через Xcode
# Запустить через Flutter
flutter run -d <device_id>

# Или через Xcode
open ios/Runner.xcworkspace
```

## 🔍 Просмотр логов

```bash
# Вывод логов приложения
flutter logs

# Логи с тегом
flutter logs | grep subscription

# Логи Android
adb logcat

# Логи iOS
log stream --predicate 'eventMessage contains "Flutter"'
```

## 📊 Анализ кода

```bash
# Анализ кода на проблемы и предупреждения
flutter analyze

# Форматирование кода
dart format lib/

# Проверка стиля
flutter pub global activate dart_code_metrics
dcm analyze lib/
```

## 🎯 Оптимизация производительности

```bash
# Запуск в режиме профилирования
flutter run --profile

# Запуск в режиме release (оптимизированная сборка)
flutter run --release
```

## 📝 Хранение данных

Приложение использует SharedPreferences для сохранения:

```
Android: /data/data/package_name/shared_prefs/
iOS: ~/Library/Application Support/package_name/
```

Данные автоматически сохраняются при выборе подписки.

## 🔄 Сброс приложения к начальному состоянию

```dart
// Если нужно сбросить данные программно:
final service = SubscriptionService();
await service.init();
await service.clearSubscription();
await service.setOnboardingComplete(false);
```

Или полностью удалите приложение:

```bash
# Android
adb uninstall com.example.mst_test_app

# iOS
# Удалите приложение с симулятора вручную
```

## 📚 Дополнительные ресурсы

- [Flutter Documentation](https://flutter.dev/docs)
- [Dart Documentation](https://dart.dev/guides)
- [shared_preferences Package](https://pub.dev/packages/shared_preferences)
- [Material Design](https://material.io/design)

## 🆘 Получение помощи

Если возникли проблемы:

1. Проверьте логи: `flutter logs`
2. Запустите анализ кода: `flutter analyze`
3. Очистите проект: `flutter clean && flutter pub get`
4. Перезагрузитесь: `flutter run -v`

---

**Готово к запуску!** 🎉

Приложение полностью функционально и готово к использованию. Наслаждайтесь!
# 🏗️ Архитектура Subscription App

## Обзор

Subscription App - это Flutter приложение для управления подписками, построенное с использованием современных паттернов разработки. Архитектура основана на разделении ответственности и простоте расширения.

## Структура проекта

```
lib/
├── main.dart                           # Точка входа и навигация
├── constants/
│   └── colors.dart                     # Палитра цветов и константы
├── models/
│   └── subscription.dart               # Бизнес-логика подписки
├── screens/
│   ├── onboarding_screen.dart          # Экран приветствия (2 шага)
│   ├── paywall_screen.dart             # Экран выбора тарифа
│   └── home_screen.dart                # Главный экран аккаунта
├── services/
│   └── subscription_service.dart       # Сервис работы с хранилищем
└── widgets/
    └── subscription_card.dart          # Переиспользуемый виджет карточки
```

## Компоненты системы

### 1. Models (models/subscription.dart)

**Ответственность**: Бизнес-логика и представление данных подписки.

**Класс: SubscriptionType** - Enum с тремя значениями: `none`, `monthly`, `yearly`

**Класс: Subscription** - Основная модель подписки

Методы:
- `isActive` - проверка активности подписки
- `displayName` - человеческое название
- `nextPaymentDate` - расчет даты следующего платежа
- `daysUntilNextPayment` - количество дней до платежа
- `paymentPeriod` - "ежемесячно" или "ежегодно"
- `getNextPaymentInfo()` - информация с правильным склонением
- `toJson() / fromJson()` - сериализация для хранения

### 2. Services (services/subscription_service.dart)

**Ответственность**: Управление хранилищем через SharedPreferences

Методы:
- `init()` - инициализация
- `saveSubscription()` - сохранить подписку
- `getSubscription()` - получить подписку
- `clearSubscription()` - удалить подписку
- `setOnboardingComplete()` - отметить онбординг завершенным
- `isOnboardingComplete()` - проверить статус онбординга
- `hasActiveSubscription()` - проверить наличие активной подписки

Хранилище:
- `subscription_data` - JSON с информацией о подписке
- `onboarding_complete` - Boolean флаг

### 3. Constants (constants/colors.dart)

**Ответственность**: Единая палитра цветов приложения

Цветовая схема:
- Primary: #6366F1 (Индиго)
- Secondary: #10B981 (Зелень)
- Background: #F9FAFB (Светло-серый)
- Text: #1F2937 (Темно-серый)
- Error/Discount: #DC2626 (Красный)

### 4. Screens

#### OnboardingScreen
- PageView для листания между экранами
- 2 экрана приветствия
- Индикаторы прогресса
- Кнопка "Продолжить" → "Начать"

#### PaywallScreen
- Две карточки подписок (месячная/годовая)
- Визуализация скидки (20% для годовой)
- Список преимуществ подписки
- Эмуляция процесса покупки
- Управление состоянием выбора

#### HomeScreen
- Карточка подписки с градиентом
- Информация о дате активации
- Дата следующего платежа
- Периодичность платежей
- Время до следующего платежа
- Список преимуществ подписки
- Кнопка логаута

### 5. Widgets

#### SubscriptionCard
Переиспользуемая карточка для выбора подписки

Параметры:
- `type` - тип подписки
- `title` - название
- `price` - цена
- `period` - период
- `discount` - строка скидки (опционально)
- `isSelected` - выбрана ли карточка
- `onTap` - обработчик нажатия

Особенности:
- Анимированные границы при выборе
- Тень, зависящая от состояния
- Бейдж скидки в углу

## Управление состоянием и навигация

### State Management

```
RootScreen (управляет состоянием)
    ↓
AppState (enum: onboarding, paywall, home)
    ↓
_buildScreen() - выбирает правильный экран
```

### Жизненный цикл приложения

```
main() 
  ↓
SubscriptionService.init()
  ↓
MyApp (root)
  ↓
RootScreen (инициализация)
  ↓
AppNavigationWrapper (определение состояния)
  ↓
[OnboardingScreen] → [PaywallScreen] → [HomeScreen]
```

### Переходы между экранами

```
OnboardingScreen
  │ onComplete()
  ↓
PaywallScreen
  │ onSubscriptionSelected(type)
  ↓
HomeScreen
  │ onLogout()
  ↓
OnboardingScreen (полный цикл)
```

## Поток данных

1. **Инициализация**
   - init() в SubscriptionService
   - Загрузка данных из SharedPreferences

2. **Выбор подписки (PaywallScreen)**
   - Создание объекта Subscription
   - Сохранение через SubscriptionService
   - setState() обновляет AppState

3. **Отображение (HomeScreen)**
   - FutureBuilder загружает подписку
   - Вычисления даты платежа в модели
   - Форматирование информации

4. **Выход (Logout)**
   - clearSubscription() удаляет данные
   - setState() возвращает на OnboardingScreen

## Обработка ошибок

### Уровни обработки

1. **Service Level** - Try-catch при десериализации JSON
2. **Model Level** - Валидация типа подписки
3. **UI Level** - FutureBuilder для асинхронных операций

## Расширяемость

### Добавление новой подписки

1. Добавить тип в `SubscriptionType enum`
2. Обновить методы в `Subscription` класс
3. Добавить карточку в `PaywallScreen`
4. Обновить информацию в `HomeScreen`

### Интеграция платежных систем

1. Создать `PaymentService` класс
2. Заменить эмуляцию в `PaywallScreen._processPurchase()`
3. Обработать результаты платежей

## Производительность

### Оптимизации

1. **Асинхронность**
   - FutureBuilder предотвращает блокировку UI
   - Асинхронные операции в SubscriptionService

2. **Кэширование**
   - SharedPreferences кэширует данные
   - FutureBuilder не перезагружает без необходимости

3. **Визуальные эффекты**
   - AnimatedContainer для плавных анимаций
   - PageView для эффективной навигации

## Безопасность

### Хранение данных

- SharedPreferences хранит данные локально
- Данные недоступны для других приложений
- JSON используется для структурирования

## Заключение

Архитектура приложения построена на принципах:

1. **Разделение ответственности** - каждый компонент имеет четкую роль
2. **Переиспользуемость** - компоненты легко комбинируются
3. **Простота** - понятный и прямолинейный код
4. **Расширяемость** - легко добавлять новые функции
5. **Локализация** - полная поддержка русского языка

Эта архитектура обеспечивает надежную основу для разработки и может быть масштабирована по мере роста приложения.